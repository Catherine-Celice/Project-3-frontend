export interface PetProfileTest {
    q1: number;     // caring/logical
    q2: number;     // rule follower/tolerant
    q3: number;     // sensitive/tough
    q4: number;     // impatient/easy going
    q5: number;     // exercises often/doesn't
    q6: number;     // doesn't require lots of petting/wants lots of petting
    q7: number;     // home alone for over 6 hours/not
    q8: number;     // shy or afraid of new people/not
    q9: number;     // quiet evening/social outing
    q10: number;    // empathetic/not
    q11: number,    // active play daily/not
    q12: number;    // advanced training/not
    q13: number;    // always doing something/not
    q14: number;    // plays outside/snuggles on couch
    q15: number;    // part of family/exercise buddy
    q16: number;    // advanced training/affectionate
    q17: number;    // loyal friend/go many places
    q18: number;    // easy-going/high-energy
    q19: number;    // home alone/not
    q20: number;    // messy/not
    q21: number;    // fast fetch/not
    q22: number;    // interested in learning important to me/not
    q23: number;    // someone is always with my pet/not
    q24: string;    // multiple choices allowed

  };

